[pytest]
testpaths = tests
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
markers =
    unit: быстрые модульные тесты без поднятия всего приложения
    integration: интеграционные тесты HTTP API и БД
    slow: длительные тесты (e2e, нагрузка)
